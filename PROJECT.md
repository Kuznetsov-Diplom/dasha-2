# Dasha v2 — Нейросетевой преобразователь «биометрия-код» (НПБК) по ГОСТ Р 52633.5-2011

** Версия:** 2.1 (17 мая 2026)  
** Статус:** Полная спецификация НПБК готова к реализации  
** Автор:** Grok (xAI) + Kuznetsov-Diplom

---

## Обзор проекта

Dasha v2 — современная система генерации криптографических ключей на основе голосовой биометрии по требованиям ГОСТ Р 52633.

** Ключевые особенности v2:**
- Чистый 13-мерный вектор (mean MFCC)
- Глобальная нормализация `global_minmax_abs`
- Полная реализация НПБК по ГОСТ Р 52633.5-2011
- Линейный морфинг для размножения
- Шифрование секрета ключом пользователя через **Кузнечик** (GOST R 34.12-2015)

---

## Полная спецификация НПБК по ГОСТ Р 52633.5-2011

(Полный текст из моего предыдущего сообщения — закреплен ниже)

### 1. Общая архитектура

НПБК — двухслойная структура (не классическая нейросеть с backpropagation).

- **Слой 1** — основной (выделение битов ключа). Каждый нейрон отвечает ровно за один бит выходного ключа (64 / 128 / 256 бит).
- **Слой 2** — корректирующий (исправляет ошибки первого слоя).

Обучение **раздельное** для каждого нейрона и каждого слоя — полностью автоматическое, по формулам стандарта.

** Цель:**
- На «Свой» → стабильный детерминированный ключ
- На «Чужой» → случайный, непредсказуемый результат

### 2. Входные данные

- **Размерность**: строго **13** (mean MFCC 1–13)
- Нормализация: `global_minmax_abs` (вектор в [0, 1])
- RASTA и Delta — **отключены**

### 3. Размножение образов (линейный морфинг)

** Метод**: Линейный морфинг двух образов-родителей (ГОСТ Р 52633.2)

** Формула:**
X_k = A + t_k · (B − A)

gde:
- A, B — два реальных вектора пользователя
- t_k = k / (num_children + 1)

** Как используем:**
- Берём все возможные пары реальных записей
- От каждой пары генерируем 4–6 морф-детей

### 4. Обучение Слоя 1 (Основной слой)

** Формула (4) — Качество признака:**
Q(v_i) = |E_чужой(v_i) − E_свой(v_i)| / σ_свой(v_i)

** Формула (6) — Модуль веса:**
|μ_i| = Q(v_i) / σ_чужой(v_i)

** Знак веса (7):**
- Цель «1» на «Свой» → sign(μ_i) = sign(E_свой − E_чужой)
- Цель «0» на «Свой» → sign(μ_i) = −sign(E_свой − E_чужой)

** Формула (2) — Качество нейрона:**
Q(y) = |E_чужой(y) − E_свой(y)| / σ_свой(y)

** Формула (3) — Количество входов:**
n ≈ a_{0} [ Q(y) / E(Q(v_i)) ]^{2}

** Смещение (bias):**
μ_{0} = E_чужой ( Σ μ_i · v_i )

### 5. Обучение Слоя 2

** Формула (8):**
μ_i = (a_{2} · ω_i) / E(ω_i)

gde:
- ω_i — стабильность i-го бита первого слоя
- a_{2} — стабилизирующий коэффициент

### 6. Маскирование корреляций («размытие весов»)

После обучения:
1. Генерируем 10 000 случайных векторов «Чужой».
2. Считаем средний |corr| между парами нейронов.
3. Случайно инвертируем знаки у большей доли весов.

### 7. Процесс регистрации

1. Пользователь записывает 8–12 фраз.
2. Извлекаем 13-мерные векторы.
3. Делаем линейный морфинг → большая выборка «Свой».
4. Генерируем выборку «Чужой».
5. Обучаем Слой 1 по формулам.
6. Обучаем Слой 2.
7. Выполняем маскирование корреляций.
8. Генерируем случайный ключ.
9. Шифруем секрет пользователя **Кузнечиком**.
10. Сохраняем в НБК.

### 8. Восстановление ключа

1. Пользователь произносит фразу.
2. Извлекаем 13-мерный вектор.
3. Прогоняем через Слой 1 → биты.
4. Прогоняем через Слой 2 → исправляем ошибки.
5. Получаем ключ.
6. Расшифровываем секрет ключом Кузнечик.

### 9. Хранение НБК (ПостгреСКЛ)

** Таблица `npbk_containers`:**

- user_id (TEXT, PK)
- key_bits (INT)
- layer1_weights (JSONB)
- layer1_bias (JSONB)
- layer2_weights (JSONB)
- correlation_mask (JSONB)
- encrypted_secret (BYTEA)
- created_at (TIMESTAMP)
- version (TEXT)

---

## Используемые документы

- Полный текст стандарта **ГОСТ Р 52633.5-2011** прикреплён к проекту в чате (файл `4293797154.pdf`).
- Дипломная работа «Даша» также прикреплена в чате.

---

## Следующие шаги

1. Реализовать `npbk.py` по этой спецификации
2. Обновить `pipeline.py` (13 мер, без RASTA)
3. Добавить интеграцию в `app.py` (5-я вкладка)
4. Протестировать на стабильность ключа

** Готов к реализации.**